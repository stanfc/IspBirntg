# IspBirntg 環境設定
# 複製此檔案為 .env 並根據需要修改設定

# 後端服務 Port (預設: 8080)
BACKEND_PORT=8080

# 前端服務 Port (預設: 5173)
FRONTEND_PORT=5173

# 可選：完整的後端 API URL（如果需要覆蓋預設值）
# VITE_BACKEND_URL=http://localhost:8080/api